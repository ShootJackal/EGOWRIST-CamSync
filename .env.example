# Default bridge URL - used on first app launch before user sets it in Settings
# Leave blank in production; user enters bridge URL in the Settings screen
EXPO_PUBLIC_DEFAULT_BRIDGE_URL=

# For local development only - set to your Mac's LAN IP and bridge port
# EXPO_PUBLIC_DEFAULT_BRIDGE_URL=http://192.168.1.50:4000
